meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/users/login
  body: json
  auth: none
}

body:json {
  {
    "email":"luans@example.com",
    "password":"password123Ls"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}
